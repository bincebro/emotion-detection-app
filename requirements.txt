# ── Web Framework ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9       # for file uploads

# ── Computer Vision ────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0
Pillow>=10.2.0
numpy>=1.26.0

# ── Emotion Detection Backends ─────────────────────────────────────────────
deepface>=0.0.90              # primary: uses RetinaFace + DeepEmotion
fer>=22.5.1                   # fallback: MTCNN + Mini-Xception

# ── Deep Learning (for DeepFace & custom training) ─────────────────────────
tensorflow>=2.15.0            # required by deepface
# tensorflow-macos>=2.15.0   # uncomment if on Apple Silicon M1/M2

# ── Custom Model Training ─────────────────────────────────────────────────
torch>=2.2.0                  # comment out if only using deepface
torchvision>=0.17.0
onnx>=1.15.0                  # model export
onnxruntime>=1.17.0           # fast CPU inference

# ── Data & Training Utils ─────────────────────────────────────────────────
pandas>=2.2.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
tf-keras>=2.18.0

# ── Kaggle (for downloading datasets) ────────────────────────────────────
kaggle>=1.6.0                 # pip install kaggle; needs ~/.kaggle/kaggle.json
